<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Only the exports subfolder of the cache dir is exposed - nothing
         else on disk is reachable through the FileProvider. -->
    <cache-path name="exports" path="exports/" />
</paths>
